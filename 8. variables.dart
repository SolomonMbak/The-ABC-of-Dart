main() {
  /* variable declerations
  We will declare 2 variables using 2 different keywords.
  Using 'var', you can assign variables of any type.
  Using string, only string data type can go in.
  */
  var firstName = 'Solomon';
  String otherNames = 'Mbak Joshua';
  int anInt; //This line will return a null value.
  String aString = null; //This will also give a null output

  // print both variables
  print('My name is: ' + firstName + ' ' + otherNames);
  print(anInt);
  print(aString);
}